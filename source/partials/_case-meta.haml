.row
  .case-top.scene_element.scene_element--fadeInDownDelay
    .case-top-content{ :style => 'background-color:' + locals[:color] + ';' + 'color:' + locals[:fontcolor], }
      %h1.scene_element.scene_element--fadeInDownDelayDelay
        = locals[:h1]
        %sup= locals[:year]
      .case-meta
        .case-tags
          %ul
            %p Tags
            - current_page.data.tags.each do |f|
              %li
                %p= f
        .case-role
          %ul
            %p Roles
            - current_page.data.role.each do |f|
              %li
                %p= f
