.case-top{ :style => 'background-color:' + locals[:color] }
  .row
    %h1
      = locals[:h1]
      %sup= locals[:year]
    %ul
      - current_page.data.tags.each do |f|
        %li= f
    %ul
      - current_page.data.role.each do |f|
        %li= f
