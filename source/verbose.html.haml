.intro.ingress.scene_element.scene_element--fadeInDownDelay
  - blog('verbose').articles[0...5].each do |article|
    %case{:href => article.url}
      %h3
        = article.title
      %p= article.date.strftime('%Y-%m-%d')
      %p= article.summary(500)
      %a= link_to 'Read more &rarr;', article.url
